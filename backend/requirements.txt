# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# Settings
pydantic-settings==2.7.1

# Deepgram
deepgram-sdk==3.9.0

# LLM Providers
groq==0.15.0
google-generativeai==0.8.4
httpx==0.28.1

# Validation
rapidfuzz==3.11.0
numpy==2.2.6
# Semantic similarity (Layer 3) uses Gemini's hosted embedding model via
# google-generativeai above, not a local model — was sentence-transformers,
# swapped out to drop the torch dependency (~1-2GB installed, 300-500MB+
# resident) for the deploy. See services/validation.py's module docstring.

# PII redaction
regex==2024.11.6

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0

# Synthetic call generation (scripts/generate_sample_calls.py)
edge-tts==7.2.8
pydub==0.25.1
